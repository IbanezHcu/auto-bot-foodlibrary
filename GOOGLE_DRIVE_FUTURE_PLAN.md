# Google Drive future plan

Add a read-only ingestion adapter using least-privilege OAuth, folder allowlists, stable file IDs, revision/checksum tracking and approval before publish. Drive is not connected and is never the search source of truth directly.
