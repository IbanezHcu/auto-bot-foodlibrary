# Voice future plan

The disabled microphone reserves the UI. A future adapter requests permission only after click, converts speech to text, does not retain audio by default, shows privacy notice and sends the transcript through the same permission-aware search path.
