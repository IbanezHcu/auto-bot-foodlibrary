# Restore guide

Restore only into an isolated test target first. Verify manifest and SHA-256, import D1 into a fresh database, copy R2 objects to a fresh bucket, bind a protected test deployment, run permission/search/citation tests, record evidence, then request production change approval. Never overwrite production during a drill.
