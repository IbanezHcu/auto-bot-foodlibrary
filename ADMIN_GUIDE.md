# Admin guide

The current Admin screen is a non-writing preview. Do not enable write actions until Auth and role authorization are configured. Production workflow is Draft → Review → Approve → Publish → Archive. Owner, status, version, update date, review date and history are mandatory. Reviewer and Approver actions must be attributable and auditable.
